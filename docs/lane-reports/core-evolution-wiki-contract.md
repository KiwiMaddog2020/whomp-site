# Core-evolution wiki evidence contract

Role: site-only worker. Branch `codex/core-evolution-wiki-contract`, worktree `/private/tmp/whomp-site-core-evolution-wiki`, base `06934f5e551e79d70fb5235ea0e408f92013ad28`. No site-specific AGENTS.md exists; global scoped-worker policy applies. No game file, artifact, generated site page or publishing configuration was modified.

The live game's exact candidate remains `d0eafb70357fcb39bc245c02e269b0b3f64a5c3f`. Companion-site generation failed eight evidence checks because its September13 validator interpreted six September20 core-evolution registry faces as base automatic weapons. Thirty legal automatic weapons have 435 exhaustive pairs; the old site predicate counted 36 and demanded 630. Canonical tier coverage is 55 definitions, 40 measured, 100 rows, and 15 explicitly unmeasured (nine disabled donors plus six aimed-core faces). The data is complete. The September20 sweep commit `a152b45708d94db7273f377b85da7f915d27d8d0` is an ancestor of the original game train base `33e5e0371bce8df0a17949c475f44653db45c224`; this integration did not change the tier engine, data layer, tier artifact, weapons or evolution registry.

Only the two site derivations changed: eligible base automatic IDs exclude valid core-evolution faces, and explicit unmeasured coverage includes those faces. A face must be marked evolved and refer to an existing canonical core. All exact roster, pair coverage, loadout, sampling, axes and build checks remain intact. The validator is exported for lightweight direct contract tests.

Validation: `WHOMP_REPO=/Users/kevin/whomp node --test tests/wikiTierEvidence.test.mjs` passed all five tests, zero skips, in 283 ms. Canonical 435-pair data passes; deliberately injecting a core face into the automatic roster, dropping a legal pair, dropping an unmeasured core face, and breaking the core reference each fail their relevant contract. Log: `/private/tmp/whomp-art-release/site-core-evidence-tests.log`. `git diff --check` passed. The focused suite uses canonical artifacts and reports an explicit skip when a site-only clone has no game checkout; `WHOMP_REPO` selects the checkout like the generator's `--repo` argument.

Remaining: independent review of the exact commit, site integration by the assigned publisher/integrator, full site generation and site checks before companion-site publication. No rendering/build ran while desktop packaging was active. No push or deployment was performed. Generated pages intentionally remain untouched.

## Source-render contract follow-up

The normal publisher retry after the initial correction advanced to seven source-render refusals: all six aimed-core forms lacked an automatic `evolvesFrom` link, and Powder Trail lacked an automatic `tickRateMs`. Both fields would be false evidence for aimed-core registry faces. The renderer now uses the canonical `evolvesFromCore` origin, links the core and paired tome, and describes aimed-core rhythm without donor damage or an invented tick interval. The independent evolution recipe determines which backlink is mandatory, preserving the deletion detector; only independently identified core recipes are exempt from automatic tick cadence. No canonical data or generated files changed.

Expanded validation calls complete `buildWiki` with the actual canonical game/tier/visual artifacts and inert page chrome. All evidence, visual and source contracts now pass together, and every page renders in memory. Every core card links its actual core and has no automatic cadence/base-damage claim. Removing any core origin still fails, as does deleting a real automatic ticker's tick interval. `WHOMP_REPO=/Users/kevin/whomp node --test tests/wikiTierEvidence.test.mjs`: eight passed, zero skipped, 458 ms. Log `/private/tmp/whomp-art-release/site-core-render-tests.log`. No build, browser, game write or publication ran. Exact follow-up review and publisher's actual site-generation/check sequence remain required.

Independent review found a P2: a wrong but existing core reference could have rendered a false origin while satisfying presence-only checks. The tier/source admission now requires exactly one evolution recipe for each evolved definition, an existing base, and exact equality with the appropriate core or automatic backlink, rejecting the opposite backlink too. This runs before visual admission so missing/duplicate recipes cannot be hidden by unrelated visual refusals. Positive card expectations derive their origin from the recipe, not the backlink being tested. New negatives substitute another existing core for every face and remove, duplicate or break a recipe. Final focused run: ten passed, zero skips, 574 ms (`site-core-render-tests-v3.log`). The intermediate v2 log records a test expecting a later source error while the earlier visual guard correctly refused the missing recipe; validation was moved to admission, not weakened. No game/source data or generated pages changed.
